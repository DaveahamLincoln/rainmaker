<%= form_for(@shasum) do |f| %>
  <% if @shasum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shasum.errors.count, "error") %> prohibited this shasum from being saved:</h2>

      <ul>
      <% @shasum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sum %><br>
    <%= f.text_field :sum %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
